defaults:
  top_k: 5
  num_hops: 2
  num_seen_min: 1
presets:
  specific: {top_k: 3, num_hops: 1, num_seen_min: 1}
  relational: {top_k: 5, num_hops: 2, num_seen_min: 1}
  broad: {top_k: 8, num_hops: 2, num_seen_min: 2}
  multi_hop: {top_k: 8, num_hops: 3, num_seen_min: 2}
